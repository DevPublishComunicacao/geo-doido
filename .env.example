# Configuração do Banco de Dados (PostgreSQL via Docker)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=where
DB_USER=where
DB_PASSWORD=where123

# JWT
JWT_SECRET=where-jwt-secret-dev

# Sessão
SESSION_SECRET=where-session-secret

# URL do Frontend
FRONTEND_URL=http://localhost:3000

# SMTP (para envio de emails de recuperação de senha)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=seuemail@gmail.com
# SMTP_PASS=suasenhaapp
# SMTP_FROM="Where? <noreply@wheregame.com>"

# Google OAuth
# GOOGLE_CLIENT_ID=seu-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=seu-client-secret
